[build-system]
requires = ["setuptools>=61.0"]
build-backend = "setuptools.build_meta"

[project]
name = "insolvency-tool"
version = "0.1.0"
description = "Werkzeug zur Verwaltung von Verbraucherinsolvenzverfahren"
readme = "README.md"
authors = [{ name = "AutoGenerated" }]
requires-python = ">=3.10"
dependencies = []

[project.scripts]
insolvency = "insolvency.cli:main"
